---
phase: 02-parallel-execution-engine
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - autopilot/src/orchestrator/index.ts
  - autopilot/src/orchestrator/__tests__/unified-loop.test.ts
autonomous: true
gap_closure: true
requirements: [EXEC-01, EXEC-02, GIT-01]
must_haves:
  truths:
    - "In parallel mode, each worker's ClaudeService instance is used for phase execution, not the shared orchestrator ClaudeService"
    - "In parallel mode, each worker's worktree cwd is passed to all ClaudeService.runGsdCommand calls, not this.projectDir"
    - "In sequential mode, behavior is unchanged -- this.claudeService and this.projectDir are used as before"
  artifacts:
    - path: "autopilot/src/orchestrator/index.ts"
      provides: "runPhase, runStep, executeWithRetry accept optional claudeService and cwd overrides"
      contains: "claudeService?: ClaudeService"
  key_links:
    - from: "orchestrator dispatch callback (line 409)"
      to: "runPhase"
      via: "passes cwd and claudeService arguments"
      pattern: "this\\.runPhase\\(phaseState,\\s*\\w+,\\s*\\w+"
    - from: "runPhase"
      to: "executeWithRetry"
      via: "passes claudeService and cwd through runStep closures"
      pattern: "executeWithRetry\\(.*claudeService"
---

<objective>
Wire worker-specific ClaudeService and cwd through the orchestrator's phase execution chain.

Purpose: Close the last-mile gap where WorkerPool creates per-worker ClaudeService instances and worktree paths but the dispatch callback ignores them, causing all parallel workers to share one ClaudeService and run in the main repo directory.

Output: The dispatch callback passes cwd and claudeService to runPhase, which threads them through runStep, executeWithRetry, and all lifecycle methods (runDiscuss, runPlan, runExecute, runVerifyWithGapLoop).
</objective>

<execution_context>
@C:/Users/russw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/russw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallel-execution-engine/02-03-SUMMARY.md
@.planning/phases/02-parallel-execution-engine/02-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From autopilot/src/worker/index.ts:
```typescript
export type RunPhaseFn = (cwd: string, claudeService: ClaudeService) => Promise<void>;
```
WorkerPool.dispatch(phase, runPhaseFn) calls runPhaseFn(cwd, claudeService) on line 140.
In parallel mode, cwd = worktree path. In sequential mode, cwd = projectDir.
claudeService is always a fresh per-worker instance created on line 57.

From autopilot/src/orchestrator/index.ts (current dispatch callback, line 409):
```typescript
workerPool.dispatch(readyPhase, async (_cwd, _claudeService) => {
  // TODO (Phase 3): In parallel mode, pass worker-specific ClaudeService
  // For now, sequential mode uses the shared this.claudeService via runPhase
  await this.runPhase(phaseState);
});
```

Current method signatures that need overrides:
```typescript
private async runPhase(phase: PhaseState): Promise<void>
private async runStep(phase: PhaseState, stepName: PhaseStep, fn: () => Promise<void>): Promise<void>
private async executeWithRetry(prompt: string, meta: { phase: number; step: string; timeoutMs?: number; maxTurns?: number }): Promise<CommandResult>
```

Methods called from runPhase that use this.projectDir:
- runDiscuss: writeSkipDiscussContext(this.projectDir, ...), join(this.projectDir, '.planning', 'phases'), this.executeWithRetry(...)
- runPlan: join(this.projectDir, '.planning', 'phases'), this.executeWithRetry(...)
- runExecute: this.executeWithRetry(...)
- runVerifyWithGapLoop: this.executeWithRetry(...), checkForGaps(this.projectDir, ...)
- runStep: this.getGitHead() [uses this.projectDir], this.getNewCommits() [uses this.projectDir]
- executeWithRetry: this.claudeService.runGsdCommand(prompt, { cwd: this.projectDir, ... }) on lines 944 and 969

Methods called from runStep that use this.projectDir:
- getGitHead: execFile('git', ['rev-parse', 'HEAD'], { cwd: this.projectDir })
- getNewCommits: execFile('git', ..., { cwd: this.projectDir })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread claudeService and cwd overrides through orchestrator method chain</name>
  <files>autopilot/src/orchestrator/index.ts, autopilot/src/orchestrator/__tests__/unified-loop.test.ts</files>
  <behavior>
    - Test 1: In parallel mode (concurrency > 1), the dispatch callback passes worker cwd and claudeService to runPhase (not underscored/ignored)
    - Test 2: In sequential mode (concurrency = 1), runPhase still uses this.claudeService and this.projectDir (backward compatible)
    - Test 3: executeWithRetry uses the provided claudeService parameter (not this.claudeService) when one is given
    - Test 4: executeWithRetry uses the provided cwd parameter (not this.projectDir) when one is given
  </behavior>
  <action>
    1. Add optional parameters to the method chain in orchestrator/index.ts:

    a) `runPhase(phase: PhaseState, overrideClaudeService?: ClaudeService, overrideCwd?: string)`:
       - Create local aliases: `const cs = overrideClaudeService ?? this.claudeService` and `const cwd = overrideCwd ?? this.projectDir`
       - Pass cs and cwd to all downstream calls

    b) `executeWithRetry(prompt, meta, claudeService?: ClaudeService, cwd?: string)`:
       - Use `(claudeService ?? this.claudeService).runGsdCommand(prompt, { cwd: cwd ?? this.projectDir, ... })` on both attempt lines (944, 969)

    c) `runStep(phase, stepName, fn)` does NOT need signature changes -- it receives overrides via the closure from runPhase. However, `getGitHead()` and `getNewCommits()` are called directly inside runStep, so add optional `cwd?: string` to runStep and thread it to those calls.

    d) For `getGitHead()` and `getNewCommits()`: add optional `cwd?: string` parameter, defaulting to `this.projectDir`.

    e) For lifecycle methods called from runPhase (runDiscuss, runPlan, runExecute, runVerifyWithGapLoop): add optional `claudeService?: ClaudeService` and `cwd?: string` parameters. Thread them to executeWithRetry and to any join(this.projectDir, ...) calls. Specifically:
       - runDiscuss: replace `this.projectDir` with `cwd ?? this.projectDir` in writeSkipDiscussContext and join() calls, pass cs and cwd to executeWithRetry
       - runPlan: replace `this.projectDir` with `cwd ?? this.projectDir` in join() calls, pass cs and cwd to executeWithRetry
       - runExecute: pass cs and cwd to executeWithRetry
       - runVerifyWithGapLoop: pass cs and cwd to executeWithRetry and checkForGaps, add cwd to runStep calls

    2. Update the dispatch callback (line 409) to pass through the parameters:
    ```typescript
    workerPool.dispatch(readyPhase, async (cwd, claudeService) => {
      await this.runPhase(phaseState, claudeService, cwd);
    });
    ```
    Remove the TODO comment about Phase 3.

    3. Verify the abortAll wiring -- workerPool.abortAll() should already be called during shutdown. The shutdown handler at line 458 calls `this.claudeService.abortCurrent()` for the shared instance. For parallel mode, WorkerPool.abortAll() handles the per-worker instances. Check that workerPool is accessible from the shutdown path -- if not, wire it.

    4. Add/update tests in unified-loop.test.ts:
       - Add a test that verifies in parallel mode, the ClaudeService instance passed to runGsdCommand is the worker-specific one (not the shared orchestrator one). This can be verified by checking that the mocked ClaudeService created by WorkerPool receives the runGsdCommand call, not the one passed to the Orchestrator constructor.
       - Add a test that verifies the cwd passed to runGsdCommand is the worktree path in parallel mode
       - Verify existing sequential tests still pass unchanged

    The simplest approach for runStep: add optional `cwd?: string` parameter. From runPhase, call `this.runStep(phase, 'discuss', () => this.runDiscuss(phase, cs, cwd), cwd)`.
  </action>
  <verify>
    <automated>cd autopilot && npx vitest run src/orchestrator/__tests__/unified-loop.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - dispatch callback uses cwd and claudeService (not _cwd and _claudeService)
    - runPhase accepts and threads optional claudeService and cwd to all downstream methods
    - executeWithRetry uses provided claudeService/cwd when given, falls back to this.claudeService/this.projectDir when not
    - All existing tests pass, new tests verify parallel wiring
    - Sequential mode behavior is completely unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Full test suite regression check</name>
  <files>autopilot/src/orchestrator/index.ts</files>
  <action>
    Run the complete test suite to verify no regressions. The changes in Task 1 add optional parameters with defaults matching existing behavior, so all existing callers should work without modification.

    If any test fails:
    1. Check if the failure is in orchestrator.test.ts -- the existing tests call runPhase indirectly through run(), which goes through the dispatch callback. Since sequential mode uses the same this.claudeService, these should still pass.
    2. Check if the failure is in unified-loop.test.ts -- the mocked ClaudeService should still be used in sequential mode.
    3. Fix any issues while maintaining the parallel wiring.

    Also verify tsc --noEmit passes (type safety for the new optional parameters).
  </action>
  <verify>
    <automated>cd autopilot && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - All tests pass (882+ existing + new parallel wiring tests)
    - TypeScript compilation succeeds with no errors
    - No regressions in sequential mode behavior
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd autopilot && npx vitest run --reporter=verbose` -- all tests green
2. `cd autopilot && npx tsc --noEmit` -- no type errors
3. Grep for `_cwd` and `_claudeService` in orchestrator/index.ts -- should find ZERO matches (no longer ignored)
4. Grep for `TODO.*Phase 3.*parallel` in orchestrator/index.ts -- should find ZERO matches (TODO removed)
</verification>

<success_criteria>
- The dispatch callback on line 409 passes cwd and claudeService (not underscored) to runPhase
- runPhase threads the overrides through the entire execution chain: runStep, executeWithRetry, runDiscuss, runPlan, runExecute, runVerifyWithGapLoop
- executeWithRetry uses the worker-specific ClaudeService and cwd when provided
- getGitHead and getNewCommits use the worker-specific cwd when provided
- All lifecycle methods (discuss/plan/execute/verify) use the worker-specific cwd for file path resolution
- Sequential mode (no overrides passed) behaves identically to before
- All 882+ existing tests pass, new tests cover parallel wiring
</success_criteria>

<output>
After completion, create `.planning/phases/02-parallel-execution-engine/02-04-SUMMARY.md`
</output>
