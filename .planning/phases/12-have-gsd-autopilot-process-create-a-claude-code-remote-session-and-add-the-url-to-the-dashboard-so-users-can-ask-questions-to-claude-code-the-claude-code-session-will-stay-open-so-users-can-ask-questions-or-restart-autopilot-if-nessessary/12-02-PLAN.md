---
phase: 12-claude-code-remote-session
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - autopilot/src/server/routes/api.ts
  - autopilot/dashboard/src/types/index.ts
  - autopilot/dashboard/src/api/client.ts
  - autopilot/dashboard/src/store/index.ts
  - autopilot/dashboard/src/hooks/useSSE.ts
  - autopilot/dashboard/src/components/RemoteSessionCard.tsx
  - autopilot/dashboard/src/pages/Overview.tsx
autonomous: true
must_haves:
  truths:
    - "Dashboard Overview page shows a prominent card with the remote session URL when available"
    - "Clicking the remote session URL opens claude.ai/code in a new browser tab"
    - "Copy URL button copies the remote session URL to clipboard"
    - "Remote session card is hidden when no session URL is available"
    - "Dashboard polls /api/status and receives remoteSessionUrl to keep card in sync"
  artifacts:
    - path: "autopilot/dashboard/src/components/RemoteSessionCard.tsx"
      provides: "React component displaying remote session URL with copy button"
      min_lines: 30
    - path: "autopilot/dashboard/src/pages/Overview.tsx"
      provides: "Overview page with RemoteSessionCard below TunnelBanner"
      contains: "RemoteSessionCard"
    - path: "autopilot/dashboard/src/store/index.ts"
      provides: "remoteSessionUrl state field and setter action"
      contains: "remoteSessionUrl"
    - path: "autopilot/dashboard/src/api/client.ts"
      provides: "remoteSessionUrl in StatusResponse type"
      contains: "remoteSessionUrl"
    - path: "autopilot/src/server/routes/api.ts"
      provides: "remoteSessionUrl in /api/status response"
      contains: "remoteSessionUrl"
  key_links:
    - from: "autopilot/dashboard/src/components/RemoteSessionCard.tsx"
      to: "autopilot/dashboard/src/store/index.ts"
      via: "useDashboardStore selector for remoteSessionUrl"
      pattern: "useDashboardStore.*remoteSessionUrl"
    - from: "autopilot/dashboard/src/hooks/useSSE.ts"
      to: "autopilot/dashboard/src/api/client.ts"
      via: "fetchStatus().remoteSessionUrl -> store.setRemoteSessionUrl()"
      pattern: "setRemoteSessionUrl"
    - from: "autopilot/src/server/routes/api.ts"
      to: "autopilot/src/types/state.ts"
      via: "stateProvider.getState().remoteSessionUrl in /api/status response"
      pattern: "remoteSessionUrl"
---

<objective>
Add the remote session URL to the server API status response, extend the dashboard types/store/API client to carry the field, create a RemoteSessionCard component following the TunnelBanner pattern, and wire it into the Overview page. This makes the session URL visible and copyable from the dashboard.

Purpose: Users need to see and access the Claude Code remote session URL from the dashboard to interact with Claude about their project from any device.

Output: Server API returns remoteSessionUrl, dashboard displays it in a prominent card with copy-to-clipboard on the Overview page.
</objective>

<execution_context>
@C:/Users/russw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/russw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@autopilot/src/server/routes/api.ts
@autopilot/dashboard/src/components/TunnelBanner.tsx
@autopilot/dashboard/src/store/index.ts
@autopilot/dashboard/src/types/index.ts
@autopilot/dashboard/src/api/client.ts
@autopilot/dashboard/src/hooks/useSSE.ts
@autopilot/dashboard/src/pages/Overview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend server API, dashboard types, store, and API client with remoteSessionUrl</name>
  <files>
    autopilot/src/server/routes/api.ts
    autopilot/dashboard/src/types/index.ts
    autopilot/dashboard/src/api/client.ts
    autopilot/dashboard/src/store/index.ts
    autopilot/dashboard/src/hooks/useSSE.ts
  </files>
  <action>
**1. Extend `/api/status` response in `autopilot/src/server/routes/api.ts`:**

In the `GET /status` handler (around line 96-108), add `remoteSessionUrl` and `tunnelUrl` to the response object. Both fields come from state:

```typescript
router.get('/status', async (_req: Request, res: Response) => {
  const state = stateProvider.getState();
  const alive = livenessProvider ? await livenessProvider.isAlive() : true;
  res.json({
    status: state.status,
    currentPhase: state.currentPhase,
    currentStep: state.currentStep,
    progress: computeProgress(state),
    startedAt: state.startedAt,
    lastUpdatedAt: state.lastUpdatedAt,
    alive,
    projectName: basename(process.cwd()),
    projectDescription: readProjectDescription(),
    tunnelUrl: state.tunnelUrl,           // ADD: tunnel URL from state
    remoteSessionUrl: state.remoteSessionUrl,  // ADD: remote session URL from state
  });
});
```

Note: `tunnelUrl` was already in AutopilotState but was not being served in the status response. Adding it here fixes that gap and adds remoteSessionUrl in the same pattern.

**2. Extend `StatusResponse` in `autopilot/dashboard/src/api/client.ts`:**

Add `remoteSessionUrl?: string;` to the `StatusResponse` interface, right after the existing `tunnelUrl?: string;` line.

**3. Extend dashboard store in `autopilot/dashboard/src/store/index.ts`:**

Add to the `DashboardState` interface:
- State field: `remoteSessionUrl: string | null;` (after the `tunnelUrl` field, with comment: `// Claude Code remote session URL, null if disabled or failed`)
- Action: `setRemoteSessionUrl: (url: string | null) => void;`

Add to the `create<DashboardState>()` implementation:
- Initial state: `remoteSessionUrl: null,` (after `tunnelUrl: null,`)
- Action: `setRemoteSessionUrl: (url) => set({ remoteSessionUrl: url }),` (after `setTunnelUrl`)

**4. Wire remoteSessionUrl in `autopilot/dashboard/src/hooks/useSSE.ts`:**

In the `rehydrate()` function (around line 32), after `store.setTunnelUrl(statusRes.tunnelUrl ?? null);`, add:
```typescript
store.setRemoteSessionUrl(statusRes.remoteSessionUrl ?? null);
```

In the polling timer handler (around line 170), after `st.setTunnelUrl(s.tunnelUrl ?? null);`, add:
```typescript
st.setRemoteSessionUrl(s.remoteSessionUrl ?? null);
```

This ensures the dashboard picks up the remote session URL on initial load, reconnect, and every 3-second poll.
  </action>
  <verify>
Run `npx tsc --noEmit` from both `autopilot/` and `autopilot/dashboard/` directories -- both should compile with no errors. Grep `autopilot/src/server/routes/api.ts` for `remoteSessionUrl` to confirm it appears in the status response. Grep `autopilot/dashboard/src/store/index.ts` for `remoteSessionUrl` to confirm state field and action exist.
  </verify>
  <done>
Server `/api/status` endpoint returns `remoteSessionUrl` (and `tunnelUrl`). Dashboard store has `remoteSessionUrl` field and `setRemoteSessionUrl` action. useSSE hook reads `remoteSessionUrl` from status response and updates store on rehydrate and poll. TypeScript compiles cleanly in both projects.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RemoteSessionCard component and wire into Overview page</name>
  <files>
    autopilot/dashboard/src/components/RemoteSessionCard.tsx
    autopilot/dashboard/src/pages/Overview.tsx
  </files>
  <action>
**1. Create `autopilot/dashboard/src/components/RemoteSessionCard.tsx`:**

Create a new React component following the TunnelBanner pattern exactly (`autopilot/dashboard/src/components/TunnelBanner.tsx`). Key details:

- Import `useState` from React and `useDashboardStore` from store
- Read `remoteSessionUrl` from store using individual selector: `useDashboardStore((s) => s.remoteSessionUrl)`
- If `remoteSessionUrl` is null/undefined, return null (render nothing)
- Use a blue color scheme (to visually distinguish from TunnelBanner's purple):
  - Container: `bg-blue-50 border border-blue-200 rounded-lg p-4`
  - Icon color: `text-blue-500`
  - Title color: `text-blue-900`
  - Link color: `text-blue-700 hover:text-blue-900`
  - Button: `bg-blue-600 hover:bg-blue-700 text-white`
- Layout: same flex container as TunnelBanner (responsive row/column)
- Left side:
  - Icon: Use a terminal/code-related Unicode character (use `>_` text styled as monospace, or the code symbol). Avoid emoji per project convention. Use a simple styled span: `<span className="text-blue-500 text-lg font-mono flex-shrink-0 mt-0.5">&gt;_</span>`
  - Title: `"Claude Code remote session"` (bold, blue-900)
  - URL: anchor tag with `href={remoteSessionUrl}`, `target="_blank"`, `rel="noopener noreferrer"`, underlined, break-all
  - Subtitle: `"Ask Claude questions about your project from any device"` (smaller, blue-600)
- Right side: "Copy URL" button with clipboard copy logic (identical to TunnelBanner):
  - Click handler calls `navigator.clipboard.writeText(remoteSessionUrl)`
  - Shows "Copied!" for 2 seconds after successful copy
  - Catches clipboard errors silently (non-fatal)

**2. Wire RemoteSessionCard into Overview page (`autopilot/dashboard/src/pages/Overview.tsx`):**

- Add import at the top: `import { RemoteSessionCard } from '../components/RemoteSessionCard.js';`
- Place `<RemoteSessionCard />` immediately AFTER `<TunnelBanner />` in the JSX, so the layout order is:
  1. TunnelBanner (purple, remote dashboard access)
  2. RemoteSessionCard (blue, Claude Code interactive session)
  3. Project description
  4. No-milestone card (if applicable)
  5. Progress bar
  6. Grid (phases + logs + activity)

This placement ensures both URL cards appear at the top of the overview, grouped together visually, with the remote session card being the more prominent feature below the tunnel banner.
  </action>
  <verify>
Run `npx tsc --noEmit` from `autopilot/dashboard/` -- should compile with no errors. Verify `RemoteSessionCard.tsx` exists and contains: `useDashboardStore`, `remoteSessionUrl`, `clipboard.writeText`, `target="_blank"`, `claude.ai`. Verify `Overview.tsx` imports and renders `<RemoteSessionCard />` after `<TunnelBanner />`. Run `npm run build` from `autopilot/dashboard/` to verify the Vite build succeeds.
  </verify>
  <done>
RemoteSessionCard component renders a blue card with the remote session URL, clickable link (opens in new tab), and copy-to-clipboard button. Card is hidden when no URL is available. Overview page shows RemoteSessionCard below TunnelBanner. Dashboard builds successfully.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in both `autopilot/` and `autopilot/dashboard/`
2. `npm run build` succeeds in `autopilot/dashboard/`
3. `/api/status` response includes `remoteSessionUrl` field (from state)
4. Dashboard store has `remoteSessionUrl` field, `setRemoteSessionUrl` action
5. useSSE hook reads and sets `remoteSessionUrl` from status polling
6. RemoteSessionCard renders conditionally (null when no URL, card when URL present)
7. Overview page renders RemoteSessionCard after TunnelBanner
</verification>

<success_criteria>
- Server API returns remoteSessionUrl in /api/status endpoint
- Dashboard types, store, API client, and SSE hook all carry remoteSessionUrl
- RemoteSessionCard shows prominent blue card with URL, external link, and copy button
- Card hidden when no remote session is active (returns null)
- Overview page renders RemoteSessionCard below TunnelBanner
- Both TypeScript projects compile and dashboard builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/12-have-gsd-autopilot-process-create-a-claude-code-remote-session-and-add-the-url-to-the-dashboard-so-users-can-ask-questions-to-claude-code-the-claude-code-session-will-stay-open-so-users-can-ask-questions-or-restart-autopilot-if-nessessary/12-02-SUMMARY.md`
</output>
