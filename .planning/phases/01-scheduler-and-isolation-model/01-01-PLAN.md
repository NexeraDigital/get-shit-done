---
phase: 01-scheduler-and-isolation-model
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - autopilot/src/scheduler/parse-depends-on.ts
  - autopilot/src/scheduler/index.ts
  - autopilot/src/scheduler/__tests__/parse-depends-on.test.ts
  - autopilot/src/scheduler/__tests__/scheduler.test.ts
autonomous: true
requirements: [SCHED-02, SCHED-05, SCHED-06]

must_haves:
  truths:
    - "Given a ROADMAP.md with dependsOn fields, the scheduler produces correct topological ordering and identifies concurrent phases"
    - "The scheduler detects dependency cycles and throws a descriptive error at construction time"
    - "When a phase completes, markComplete() returns newly eligible phases whose dependencies are now satisfied"
    - "Phases with no dependsOn field are ready immediately (no implicit sequential ordering)"
    - "Missing dependency references (e.g., Phase 99) are warned but treated as satisfied"
  artifacts:
    - path: "autopilot/src/scheduler/parse-depends-on.ts"
      provides: "parseDependsOn() pure function converting raw strings to phase number arrays"
      exports: ["parseDependsOn"]
    - path: "autopilot/src/scheduler/index.ts"
      provides: "DependencyScheduler class with DAG scheduling, cycle detection, ready/complete tracking"
      exports: ["DependencyScheduler", "SchedulerPhase", "CycleError"]
    - path: "autopilot/src/scheduler/__tests__/parse-depends-on.test.ts"
      provides: "Unit tests for all dependsOn string format variations"
      min_lines: 30
    - path: "autopilot/src/scheduler/__tests__/scheduler.test.ts"
      provides: "Unit tests for DAG scheduling, cycle detection, ready/complete lifecycle"
      min_lines: 60
  key_links:
    - from: "autopilot/src/scheduler/index.ts"
      to: "autopilot/src/scheduler/parse-depends-on.ts"
      via: "import parseDependsOn"
      pattern: "import.*parseDependsOn.*from.*parse-depends-on"
    - from: "autopilot/src/scheduler/index.ts"
      to: "autopilot/src/orchestrator/index.ts"
      via: "consumes RoadmapPhase[] output (design contract, not direct import)"
      pattern: "SchedulerPhase"
---

<objective>
Build the DAG-based dependency scheduler that parses ROADMAP.md `dependsOn` strings into structured dependencies and provides topological scheduling with cycle detection.

Purpose: This is the core scheduling primitive that Phase 2's parallel execution engine will use to determine which phases can run concurrently.
Output: `parseDependsOn()` function, `DependencyScheduler` class, and comprehensive test suites for both.
</objective>

<execution_context>
@C:/Users/russw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/russw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scheduler-and-isolation-model/01-CONTEXT.md
@.planning/phases/01-scheduler-and-isolation-model/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From autopilot/src/orchestrator/index.ts:
```typescript
interface RoadmapPhase {
  number: number;
  name: string;
  completed: boolean;
  inserted: boolean;
  dependsOn: string | null;
}

export function extractPhasesFromContent(content: string): RoadmapPhase[];
```

Note: RoadmapPhase is NOT exported from orchestrator/index.ts. The scheduler should define its own SchedulerPhase interface. The bridge code (mapping RoadmapPhase[] to SchedulerPhase[]) belongs in Phase 2's orchestrator changes, not in this module.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseDependsOn string parser with tests</name>
  <files>autopilot/src/scheduler/parse-depends-on.ts, autopilot/src/scheduler/__tests__/parse-depends-on.test.ts</files>
  <behavior>
    - parseDependsOn(null) returns []
    - parseDependsOn("") returns []
    - parseDependsOn("Nothing") returns []
    - parseDependsOn("Nothing (first phase)") returns []
    - parseDependsOn("Phase 1") returns [1]
    - parseDependsOn("Phase 1, Phase 2") returns [1, 2]
    - parseDependsOn("Phase 1 and Phase 3") returns [1, 3]
    - parseDependsOn("Phases 1, 2") returns [1, 2]
    - parseDependsOn("Phase 2.1") returns [2.1] (decimal/inserted phases)
    - parseDependsOn("Phase 1, Phase 2.1, Phase 3") returns [1, 2.1, 3]
    - Duplicate numbers are deduplicated
  </behavior>
  <action>
    RED: Create test file first at `autopilot/src/scheduler/__tests__/parse-depends-on.test.ts` with all behavior cases above. Run tests -- they must fail (module doesn't exist).

    GREEN: Create `autopilot/src/scheduler/parse-depends-on.ts` with the `parseDependsOn(raw: string | null): number[]` function. Implementation per RESEARCH.md: return [] for null/empty/nothing patterns, extract all numbers via regex `\d+(?:\.\d+)?`, use parseFloat(), deduplicate with Set. Run tests -- they must pass.

    REFACTOR: Clean up if needed. Tests must still pass.

    Key constraints from CONTEXT.md:
    - Use parseFloat() consistently (not parseInt) to handle decimal phase numbers like 2.1
    - Parser lives in autopilot only -- not shared with gsd-tools
  </action>
  <verify>
    <automated>cd C:/GitHub/GetShitDone/get-shit-done/autopilot && npx vitest run src/scheduler/__tests__/parse-depends-on.test.ts -x</automated>
  </verify>
  <done>parseDependsOn handles all known string format variations from ROADMAP.md files, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DependencyScheduler class with tests</name>
  <files>autopilot/src/scheduler/index.ts, autopilot/src/scheduler/__tests__/scheduler.test.ts</files>
  <behavior>
    - Constructor accepts SchedulerPhase[] and validates no cycles (throws CycleError on cycle)
    - getReady() returns phases with all deps satisfied and not in-progress or completed
    - getReady() on a fresh scheduler with phases [1(no deps), 2(depends 1), 3(depends 1)] returns [phase 1]
    - markInProgress(1) removes phase 1 from getReady() results
    - markComplete(1) on above example returns [phase 2, phase 3] as newly eligible
    - A phase depending on Phase 99 (non-existent) warns but treats dep as satisfied (lenient)
    - Cycle detection: phases [1 depends 2, 2 depends 1] throws CycleError at construction
    - isComplete() returns true when all phases are completed
    - Phases with no dependencies are immediately ready (no implicit sequential ordering)
    - Multiple independent roots (phases with no deps) all appear in initial getReady()
  </behavior>
  <action>
    RED: Create test file at `autopilot/src/scheduler/__tests__/scheduler.test.ts` with all behavior cases. Import from `../index.js`. Tests must fail.

    GREEN: Create `autopilot/src/scheduler/index.ts` exporting:
    - `SchedulerPhase` interface: `{ number: number; name: string; dependencies: number[] }`
    - `CycleError` class extending Error (include cycle participant info in message)
    - `DependencyScheduler` class with:
      - constructor(phases: SchedulerPhase[]) -- stores phases, runs validateNoCycles()
      - getReady(): SchedulerPhase[] -- phases whose deps are all completed, not in-progress
      - markInProgress(phaseNumber: number): void
      - markComplete(phaseNumber: number): SchedulerPhase[] -- returns newly ready phases
      - isComplete(): boolean -- all phases in completed set
      - private validateNoCycles(): void -- Kahn's algorithm, throws CycleError

    Implement Kahn's algorithm per RESEARCH.md pattern (~40 lines). For missing dependency references (dep number not in phases map), log a warning and treat as satisfied per CONTEXT.md locked decision.

    Use console.warn for missing dep warnings (orchestrator will use AutopilotLogger in Phase 2 integration).

    REFACTOR: Extract any shared helpers. Tests must still pass.

    Run full scheduler test suite to confirm both files work together.
  </action>
  <verify>
    <automated>cd C:/GitHub/GetShitDone/get-shit-done/autopilot && npx vitest run src/scheduler/__tests__/ -x</automated>
  </verify>
  <done>DependencyScheduler correctly schedules phases from DAG, detects cycles, handles missing deps leniently, all tests green</done>
</task>

</tasks>

<verification>
Run full scheduler test suite:
```bash
cd autopilot && npx vitest run src/scheduler/
```
All tests pass. No regressions in existing test suite:
```bash
cd autopilot && npx vitest run
```
</verification>

<success_criteria>
- parseDependsOn handles all documented string format variations
- DependencyScheduler produces correct topological ordering from dependency DAG
- Cycle detection throws CycleError at constructor time with descriptive message
- getReady() returns only phases whose dependencies are all satisfied
- markComplete() returns newly eligible phases
- Missing dependency references are warned but treated as satisfied
- All tests green, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-scheduler-and-isolation-model/01-01-SUMMARY.md`
</output>
